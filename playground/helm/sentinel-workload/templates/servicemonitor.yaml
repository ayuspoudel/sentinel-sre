{{- if .Capabilities.APIVersions.Has "monitoring.coreos.com/v1" }}
{{- range .Values.apps }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .name }}
spec:
  selector:
    matchLabels:
      app: {{ .name }}
  endpoints:
    - port: http
{{- end }}
{{- end }}
