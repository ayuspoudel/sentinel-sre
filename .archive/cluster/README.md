### What is this?

This is a  Cluster Registry service that:

Runs as its own pod

Exposes a stable HTTP API

Uses a real database

Persists cluster intent

Is safe, idempotent, and future-proof

Will later be consumed by the Agent Controller

No agent logic. No reconciliation. No Kubernetes clients.